#!/bin/ash

[ -x /etc/init.d/rc.firewall ] && /etc/init.d/rc.firewall $1

for S in /etc/rc5.d/*
do
	case "${S##*/}" in
	README|*dbus|*udev|*elogind|*bootlogs|*rc.local) ;;
	*) [ -x "$S" ] && $S $1 ;;
	esac
done
