# su hangs if logind is not running
rm -f rootfs-complete/lib/*/security/pam_elogind.so

cat << EOF >> etc/apt/apt.conf.d/00puppy-elogind
DPkg::Post-Invoke { "rm -f /usr/lib/*/wireplumber-*/libwireplumber-module-logind.so || true"; };
EOF
chmod 644 etc/apt/apt.conf.d/00puppy-elogind
