# add an apulse stub to avoid breakage of old .pet packages that run applications with apulse (apulse $CMD)
mkdir -p usr/local/bin
cat << EOF > usr/local/bin/apulse
#!/bin/ash
exec "\$@"
EOF
chmod 755 usr/local/bin/apulse
